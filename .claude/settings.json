{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "Set-Location 'c:/Users/idana/Documents/Avi1'; git add -A; git diff --cached --quiet; if ($LASTEXITCODE -ne 0) { git commit -m 'Auto-update'; git push origin master }",
            "timeout": 30
          }
        ]
      }
    ]
  }
}

